<h1>New Series Number</h1>

<% form_for(@series_number) do |f| %>
  <%= f.error_messages %>

 	<%= render :partial => 'entry', :locals	=> {:f => f} 	%>
	<p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', series_numbers_path %>