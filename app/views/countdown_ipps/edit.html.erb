<h1>Editing Countdown IPPs</h1>

<% form_for(@countdown_ipp) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => 'entry', :locals	=> {:f => f} 	%>
  
	<p>
    <%= f.submit 'Update' %>
  </p>
<% end %>
<%= link_to 'Back', countdown_ipps_path %>